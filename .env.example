# CaseScreenChecker Environment Variables
PORT=3000
NODE_ENV=production

# Database Configuration (Optional - If using PostgreSQL or Supabase on VPS)
# DATABASE_URL=postgresql://user:password@localhost:5432/casescreenchecker
